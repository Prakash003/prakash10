#include<stdio.h>
#include<conio.h>
void main()
{
int x,y,rem,i;
printf(" Enter the values of X and Y");
scanf("%d %d",&x,&y);
printf("\n even numbers between %d %d"are, x,y );
for(i=x;i<=y;++i)
rem=i%2;
if rem==0
{
printf("\n %d",i);
}
getch();
}
